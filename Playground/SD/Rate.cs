namespace Playground.SD
{
    /*
     Functional requirements:
      - Throttle based on count requests per sec to the service
      - Throttle based on count requests from IP address    

      User can enter only 5 attempts to login
      User can create only 10 accounts per day and etc

    Non-functional requirements:
      - Highly available service
      - Should have minimal affect to overall latency
     
     */
}
